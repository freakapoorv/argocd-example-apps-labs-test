apiVersion: batch/v1
kind: job
  metadata:
    name: external-job
    annotations:
      argocd.argoproj.io/hook: presync
      
spec:
  template:
    restartPolicy: never
    containers:
    - image: busybox
      name: test
      command:
        - 'sh'
        - '-c'
        - "echo 'Running db migration...'; sleep 10; echo 'Done!'"
    
